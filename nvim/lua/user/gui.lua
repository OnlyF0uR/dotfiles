if vim.g.neovide then
    vim.g.neovide_transparency = 0.9 -- Opacity of 90%
    vim.g.neovide_cursor_animation_length = 0 -- Disable cursor animations

    vim.cmd [[
        set guifont=Operator\ Mono
    ]]
end

